export default function SingleBoard() {
  // 1.we gone make a call to the API that only return the pins asociated with this board.
  // 2. Put the array of pins in state.
  // 3.then we want to render pins on the dom.
  return (
    <div>
      <h1>Single Board view</h1>
    </div>
  );
}
